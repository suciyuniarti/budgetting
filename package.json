{
  "name": "buku-budget",
  "version": "1.0.0",
  "description": "Aplikasi budgeting event/tour",
  "main": "index.js",
  "scripts": {
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "sync:android": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0",
    "@capacitor/cli": "^6.0.0"
  },
  "capacitor": {
    "appId": "com.bukubudget.app",
    "appName": "Buku Budget",
    "webDir": "www",
    "bundledWebRuntime": false
  }
}